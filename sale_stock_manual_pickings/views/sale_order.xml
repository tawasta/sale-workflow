<odoo>

    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form.availability</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">

            <xpath expr="//button[@name='action_unlock']" position="after">
                <button name="action_launch_stock_rules" type="object" string="Create deliveries" states="sale" groups="sales_team.group_sale_salesman"/>
            </xpath>

        </field>
    </record>

</odoo>
