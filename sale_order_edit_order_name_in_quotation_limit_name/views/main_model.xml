<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- When declaring a record in XML:
            1. Place id attribute before model
            2. In field declaration name attribute is first.
            3. Group the record by model
            4. Data tag is only used to set not-updatable data (noupdate=1)
            5. Do not prefix the xmlid by the current modules name:
                (Use <record id="view_id" NOT <record id="current_module.view_id") -->


    <!-- Use the following record for a nonherited view-->
    <record id="view_id" model="ir.ui.view">
        <field name="name">view.name</field>
        <field name="model">object_name</field>
        <field name="priority" eval="16" />
        <field name="arch" type="xml">

            <!-- Content goes here-->

        </field>
    </record>

    <!-- Inherited XML: A module can extend a view only one time.

        The naming rules should be followed even when a view is inherited, the module
        name prevents xid conflicts. In the case where a view inherited has a name which
        does not follow the guidelines set above, prefer naming the inherited view after
        the original over using a name which follows the guidelines. This eases looking
        up the original view and other inheritance if they all have the same name. -->

    <!-- Use the following record for inherited view -->
    <record id="original_id" model="ir.ui.view">
        <field name="inherit_id" ref="original_module.original_id" />

    </record>


    <!-- For records of model ir.filters use explicit user_id field. -->
    <!--<record id="filter_id" model="ir.filters">
            <field name="name">Filter name</field>
            <field name="model_id">filter.model</field>
            <field name="user_id" eval="False"/>
        </record> -->

    <!-- Naming xml_ids:
            Menu: <model_name>_menu
            View: <model_name>_view_<view_type>, where <view_type> is kanban, tree...
            Action: For main action <model_name>_action. Others suffixed with _detail
            Group: <model_name>_group_<group_name>
            Rule: <model_name>_rule_<concerned_group>
    -->
</odoo>
